# Project Learnings

- Keep example images English-only by default in both `examples/images/` and `ian-xiaohei-illustrations/assets/examples/`.
- When refreshing example art, replace the canonical 8-image set rather than keeping parallel Chinese and English variants.
